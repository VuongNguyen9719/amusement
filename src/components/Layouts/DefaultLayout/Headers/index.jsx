import React from 'react';
import PropTypes from 'prop-types';

const Headers = props => {
    return (
        <div>
            
        </div>
    );
};

Headers.propTypes = {
    
};

export default Headers;